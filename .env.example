# Optional. Normal users should run /login inside the REPL instead, which writes
# to ~/.anonyagent/config.json. These env vars are for CI / scripted usage and
# override the saved config.

# ANONYAGENT_API_KEY=sk-or-v1-...
# ANONYAGENT_BASE_URL=https://openrouter.ai/api/v1
# ANONYAGENT_MODEL=anthropic/claude-sonnet-4
# ANONYAGENT_NER_MODEL=Xenova/bert-base-multilingual-cased-ner-hrl
